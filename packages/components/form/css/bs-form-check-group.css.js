
import { css } from 'lit-element';

export const BsFormCheckGroupCss = css`
    
    :host {
        position: relative;
        display: block;
        padding-left: 1.25rem;
    }
    
    :host([inline]) {
        display: -ms-inline-flexbox;
        display: inline-flex;
        align-items: center;
        padding-left: 0;
        margin-right: 0.75rem;
    }
`;
