
import { css } from 'lit-element';

export const BsNavbarBrandCss = css`

    :host span {
        display: inline-block;
        padding-top: 0.3125rem;
        padding-bottom: 0.3125rem;
        margin-right: 1rem;
        font-size: 1.25rem;
        line-height: inherit;
        white-space: nowrap;
    }

    span {
        color: var(--navbar-brand-color);
    }
`;
