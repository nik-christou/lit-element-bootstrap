
import { css } from 'lit-element';

export const BsNavbarTextCss = css`

    :host {
        display: inline-block;
        padding-top: 0.5rem;
        padding-bottom: 0.5rem;
    }

    span {
        color: var(--navbar-text-color, rgba(0, 0, 0, 0.5));
    }
`;
